[package]
name = "evezip-ui"
version.workspace = true
edition.workspace = true

[[bin]]
name = "evezip"
path = "src/main.rs"

[dependencies]
evezip-core = { path = "../evezip-core" }
evezip-engine = { path = "../evezip-engine" }
slint = "1.12"
rfd = "0.15"

[build-dependencies]
slint-build = "1.12"
